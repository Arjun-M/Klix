"""Input subsystem package.

This package groups prompt_toolkit-facing pieces of Klix: the low-level input
engine, the mode enum, and the higher-level interactive input components that
hang off `session.ui.input`.
"""
